#include "main.h"
/**
 * _islower - checks for lower case character
 * @c: the character to check
 * Return: 1 if the character c is lowercase, otherwise 0
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
